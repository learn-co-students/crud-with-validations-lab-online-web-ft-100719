<h1>Songs: </h1> 
<table style="width: 50%; text-align: left">
<thead>
<tr>
  <th>Title:</th>
  <th>Artist:</th>
  <th>Genre:</th>
  <th>Released:</th>
  <th>Release Year:</th>
</tr>
</thead><br>
<tbody>
<% @songs.each do |song| %>
  <tr>
   <td><%= song.title %></td>
   <td><%= song.artist_name %></td>
   <td><%= song.genre %></td>
   <td><%= song.released %></td>
   <td><%= song.release_year if song.released %></td>
  </tr>
<% end %> 
</tbody>
</table>